# 变更日志

本文件记录每个版本的对外可见变化。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [1.0.0] - 2026-09-15

首个版本。

### 新增

- **按规则解析在线阅读**：粘贴小说目录页 URL，按本地 JSON 规则解析出完整章节目录与章节正文。
- **两种阅读方式，可切换**：
  - 通知模式：正文逐段输出到 IDEA Notifications（默认，行为最轻量）；
  - 专用阅读面板：IDE 右侧 ToolWindow，一次显示**整章**正文，可自由滚动、可选中复制；
    顶部工具栏复用现有动作，**每个按钮都有独立图标**，并用分隔线分成
    「跳章 ｜ 导航 ｜ 字号·行距 ｜ 结束」四组；**字号与行距**可随时调整
    （面板按钮点一下即生效，设置页也可精确设定）；标题下方有一行**状态提示**
    （正在加载 / 加载失败 / 到头了）。
    面板刻意不做「当前段高亮」、也不放翻页按钮 —— 整章都在眼前时「第几段」
    不再有可视化的意义（**段游标本身照旧**，通知模式 / 阅读历史 / 书签都按段定位）。
  - 设置项「渲染方式」**二选一**（通知 / 专用面板），**改完立即生效**。
    刻意不做「两者同时」：同一段正文出现两遍没有意义，
    而且「每段最大字数」与「字号 / 行距」会互相打架。
    （早期版本存在过的第三种取值 `BOTH` 读到会映射到**面板**，
    以免当初选「两者」的人升级后悄悄丢掉面板。）
  - 设置页里**正文相关的项跟着渲染方式走**：选通知时只有「每段最大字数」可用、
    字号与行距置灰；选面板时反过来。不生效的项会置灰并附一句说明 ——
    免得「改了半天没反应、以为是插件坏了」。置灰项的值仍会保存，切回去时还在。
    这三项**紧跟在「渲染方式」下面排在一起**（原先「每段最大字数」夹在网络配置里），
    因为是同一条选择的两个分支，分散在页面两端就看不出谁跟谁有关了。
  - 面板默认 **14 号字 / 1.5 倍行距**；正文左右留白 16px，且右侧留白已扣掉滚动条宽度，
    **两边看起来一样宽**（垂直滚动条常驻，避免内容变短时右边距又变）。
- **规则文件路径显示默认位置**：设置页输入框里直接显示**当前生效的路径**，
  一个空输入框既看不出文件在哪、也看不出「留空」是什么意思。
  保存时「与默认位置等价」的路径会被归一化回空串，保住「跟随默认」语义 ——
  这样 IDEA 将来换配置目录（升级、换机器）能自动跟上，而不是钉死一个可能已失效的绝对路径。
- **阅读历史**：每次翻页自动记录进度（精确到**章 + 段**），持久化到
  `{配置目录}/novelReader/history.json`，关掉 IDEA 再打开依然在；
  可「继续阅读」「删除选中」「删除全部」。
- **章节目录对话框**：顶部筛选框可按章节名或序号过滤；**打开时自动定位到当前章**
  （选中并滚动到可见处，当前章标有「← 当前」）。
- **书签**：把当前段标记下来（可附一句备注，自动带一段正文摘录），在一本书内任意跳回。
  列表按阅读顺序（章 → 段）排列，选中后跳转；支持删除选中 / 删除全部。
  持久化到 `{配置目录}/novelReader/bookmarks.json`，最多 200 条。
  快捷键 `Ctrl+Alt+Shift+M`（添加）与 `Ctrl+Alt+Shift+B`（列表）。
  与「阅读历史」互补：历史每本书只有一条且自动记录，书签一本书可以有很多条且由你主动标注。
- **离线缓存**（默认开启）：读过的章节自动存到本地，**再读不再发请求**；
  `Tools → Novel Reader → 缓存整本书`（`Ctrl+Alt+Shift+D`）可提前把整本下下来
  （后台进行、可取消、跳过已有缓存）。缓存**连目录页一起存**，因此断网时
  仍能从「阅读历史」恢复并继续阅读；找不到上次那章时会就近退到最近的已缓存章节。
  存放在 `{配置目录}/novelReader/cache/`（一本书一个目录、一章一个文件），
  设置页可查看占用并「清空缓存」（不影响阅读历史与书签）。
- **规则可视化编辑器**：设置页「编辑规则…」可增删改规则、调整字段，
  保存前自动把原文件备份为 `rules.json.bak`。

### 健壮性

- **失败重试与指数退避**：连接超时 / 连接被拒 / 连接重置，以及 HTTP 5xx、429 会按
  `500ms → 1s → 2s → 4s`（封顶 5s，带 ±25% 抖动）重试；4xx 与「重定向超限」等
  站点明确拒绝的情况**不重试**，立刻报错。
- **站点改版自愈**：同一 URL 的多条命中规则按文件顺序作为候选（最多 3 条），
  旧规则解析不出章节时自动换下一条。
- **自定义请求头**：规则里可声明额外请求头（如固定 Referer）。
- 抗损坏：历史文件缺失 / 损坏 / 不可写，规则文件损坏，一律退化为空数据并继续工作，
  不会导致插件启动失败。缓存同理：读不出来一律视为未命中去联网，写不进去静默忽略；
  连「拿不到缓存服务」都退化为纯联网行为，绝不让缓存打断阅读。
- **改过「每段最大字数」后旧缓存自动作废**：缓存条目记着写入时的切分参数，
  不一致就重新抓取并按新设置重切，避免同一本书里新老章节的段落划分不一致。
- **所有「点了没反应」的路径都留下痕迹**：加载开始、被防重入拦下、加载失败、正文应用失败，
  都会写到面板状态行。此前这些只进通知，而通知组 `displayType=NONE` 不弹窗 ——
  「点了下一章什么都没发生」就是这么来的。

### 技术说明

- 目标平台：IntelliJ Platform **2026.1**（`sinceBuild=261`），Java 21。
- 正文渲染通过 `ReaderPresenter` 抽象与具体展示方式解耦，
  `ReaderManager` 不依赖任何 UI。
- 面板正文行距靠**视图层**实现（`LineSpacingEditorKit` 用一个把竖直 span 放大 N 倍的
  `LabelView` 顶替默认文本视图）：Swing 自带的 `StyleConstants.setLineSpacing(...)`
  在 JDK 21 的 `ParagraphView` 里**只写不读**，`JTextArea` 的 `PlainView` 行高由
  `FontMetrics` 决定、没有可覆盖的钩子。这条路上有两个**静默失败点**（判断元素类型必须用
  `Element.getName()`；改了倍数必须重建视图），已用直接量高度的测试钉死。
- 测试：457 例（另有 1 例真实站点 E2E 默认跳过，需 `-DrunRealE2E=true`）。

### 已知限制

- 单章模式（无目录页 URL）不记录阅读历史，也不能添加书签 —— 它没有稳定的书标识。
- 书签窗口只显示**当前打开**的那本书的书签；跨书跳回请用「阅读历史」。
- 缓存**只增不自动清**：目前没有容量上限或过期策略，设置页可看占用并一键清空。
- 「缓存优先」意味着站点更新了正文不会自动感知，需要清缓存后重读。
- 导出 TXT/EPUB、多书并行、正文全文搜索**尚未支持**。
- User-Agent 只在规则文件里按站配置，**没有全局开关**（不同站点要求相反，全局值会制造难查的故障）。

[1.0.0]: https://github.com/novelreader/novel-reader/releases/tag/v1.0.0
